<resources>

    <!-- Base application theme -->
    <style name="Theme.IllsCountdowns" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/textColorLight</item>
        <item name="buttonStyle">@style/CustomButton</item>
        <item name="editTextStyle">@style/CustomEditText</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomButton">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="CustomEditText">
        <!-- Set background to black in dark mode and white in light mode -->
        <item name="android:background">@color/editTextBackground</item>
        <item name="android:textColor">@color/editTextTextColor</item>
    </style>

</resources>